{
  "name": "api-automation-tests",
  "version": "1.0.0",
  "description": "API automation testing with Mocha",
  "scripts": {
    "test": "mocha tests/**/*.test.js --timeout 10000",
    "test:watch": "mocha tests/**/*.test.js --watch",
    "test:report": "mocha tests/**/*.test.js --reporter mochawesome"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "dotenv": "^16.3.1"
  }
}